# 🐧 Linux Character Device Driver Example
Простой, но полнофункциональный символьный драйвер для Linux с учебными целями. Драйвер демонстрирует базовые принципы работы с устройствами в пространстве ядра.

## 📋 Оглавление
- [Особенности](#особенности)
- [Требования](#требования)
- [Структура проекта](#структура-проекта)
- [Сборка и установка](#сборка-и-установка)
- [Использование](#использование)
- [Тестирование](#тестирование)
- [Архитектура драйвера](#архитектура-драйвера)
- [IOCTL команды](#ioctl-команды)
- [Параметры модуля](#параметры-модуля)
- [Логирование](#логирование)


## ✨ Особенности

- ✅ Полноценный символьный драйвер с поддержкой read/write/ioctl
- ✅ Динамическое выделение major номера устройства
- ✅ Автоматическое создание файла устройства в `/dev`
- ✅ Поддержка параметров загрузки модуля
- ✅ Встроенный таймер ядра
- ✅ Статистика операций чтения/записи
- ✅ Проверка границ буфера
- ✅ Подробное логирование с отладочным режимом
- ✅ Готовые тестовые программы

- ## 📦 Требования

- Linux ядро 5.x или новее
- GCC компилятор
- Заголовочные файлы ядра (`kernel-headers`)
- Права суперпользователя для загрузки модулей

## 📁 Структура проекта

├── mydriver.c # Основной код драйвера
├── Makefile # Файл сборки драйвера
├── test_mydriver.c # Интерактивная тестовая программа
├── test.c # Простая тестовая программа
├── test.sh # Автоматический тестовый скрипт
└── README.md # Эта документация


## 🔧 Сборка и установка

### 1. Сборка драйвера
```bash
make
```

### 2. Загрузка драйвера в ядро
```bash
sudo insmod mydriver.ko [параметры]
``` 
### 3. Проверка загрузки

```bash
lsmod | grep mydriver
dmesg | tail -10
```
### 4. Установка прав доступа

sudo chmod 666 /dev/mydriver

5. Выгрузка драйвера

sudo rmmod mydriver

## 🚀 Использование
Базовые операции с устройством
```bash
int fd = open("/dev/mydriver", O_RDWR);

// Запись
write(fd, "Hello", 5);

// Чтение
char buf[100];
read(fd, buf, sizeof(buf));

// Позиционирование
lseek(fd, 10, SEEK_SET);

close(fd);
```
## Пример работы с драйвером

```bash
# Запись в устройство
echo "Test data" > /dev/mydriver

# Чтение из устройства
cat /dev/mydriver

# Использование тестовой программы
./test_mydriver
```
## 🧪 Тестирование
### Автоматическое тестирование
``` bash
chmod +x test.sh
sudo ./test.sh
```

### Меню тестовой программы

```
=== Тест драйвера MYDRIVER ===
1. Прочитать из устройства
2. Записать в устройство
3. Получить статистику (IOCTL)
4. Очистить статистику (IOCTL)
5. Получить размер буфера (IOCTL)
6. Считать с разных позиций
7. Выход
```

## 🏗 Архитектура драйвера

### Основные компоненты:
```
Инициализация (mydriver_init)

Регистрация устройства

Выделение major номера

Создание файла в /dev

Инициализация таймера

Файловые операции (file_operations)

open / release - открытие/закрытие

read / write - чтение/запись

unlocked_ioctl - управление драйвером

Управление данными

Статический буфер 1024 байта

Отслеживание используемой части

Проверка границ

Таймер ядра

Периодическое срабатывание

Настраиваемый интервал

Логирование в отладочном режиме
```

## 🔌 IOCTL команды
### Драйвер поддерживает следующие команды:

```
Команда	Код	Назначение	Возвращаемые данные
GET_STATS	0x01	Получить статистику	Массив [чтения, записи, размер данных]
CLEAR_STATS	0x02	Сбросить статистику	-
GET_BUFFER_SIZE	0x03	Получить размер буфера	Размер буфера в байтах
```
### Пример использования IOCTL:
``` bash
int stats[3];
ioctl(fd, GET_STATS, stats);
printf("Reads: %d, Writes: %d, Data size: %d\n", 
       stats[0], stats[1], stats[2]);
```

## ⚙ Параметры модуля
### Драйвер поддерживает параметры при загрузке:
``` bash
sudo insmod mydriver.ko \
  debug=1 \
  timeout_ms=3000 \
  init_message="Custom startup message"
```
## 📝 Логирование
### Просмотр логов ядра:
``` bash
sudo dmesg | grep MYDRIVER
```

### Пример логов:
```
[   12.345] MYDRIVER: Initializing driver
[   12.345] MYDRIVER: Parameters: debug=1, timeout=3000ms, message='Test'
[   12.346] MYDRIVER: Driver successfully loaded! Major number = 248
[   12.346] MYDRIVER: Device available as /dev/mydriver
[   15.350] MYDRIVER: Timer triggered! Jiffies = 4294891234
[   15.350] MYDRIVER: Writing: offset=0, len=12
[   15.350] MYDRIVER: Written 12 bytes
```
